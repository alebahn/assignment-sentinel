<h1>My courses</h1>

<table>
  <tr>
    <th>Course Number</th>
    <th>Name</th>
  </tr>

<% @courses.each do |course| %>
  <tr>
    <td><%= course.course_number %></td>
    <td><%= course.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add Course', courses_path %>
