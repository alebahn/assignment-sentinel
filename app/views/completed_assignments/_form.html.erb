<%= form_for(@completed_assignment) do |f| %>
  <% if @completed_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@completed_assignment.errors.count, "error") %> prohibited this completed_assignment from being saved:</h2>

      <ul>
      <% @completed_assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :assignment_id %><br />
    <%= f.number_field :assignment_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :done_date %><br />
    <%= f.datetime_select :done_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
